

enum OPERATOR {
	IF, THEN,
	ELSE, ENDIF,
	WHILE, ENDWHILE,
	GOTO, ASSIGN, COLON,
	LBRACKET, RBRACKET,
	LQBRACKET, RQBRACKET,
	ARRAY,
	OR,
	AND,
	BITTOR,
	XOR,
	BITAND,
	EQ, NEQ,
	LEQ, GEQ,
	SHL, SHR,
	LT, GT,
	PLUS, MINUS,
	MULTIPLY, DIV, MOD
};

enum LEXEM_TYPE {
	OPERATORS, NUMBER, VARIABLE, ARRAYS
};
